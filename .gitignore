# Rust / CosmWasm
target/
artifacts/
*.wasm
*.wasm.gz
contracts/schema/raw/
sim-failures.log

# Node
node_modules/
dist/
build/
.next/
*.tsbuildinfo
.pnpm-store/
.corepack/

# React Router typegen + Playwright run artifacts (apps/web)
.react-router/
test-results/
playwright-report/

# Devnet runtime state
infra/devnet/state/

# Environment & secrets
.env
.env.*
!.env.example
# Console deployment profiles are tracked deliberately: VITE_ values are
# client-public configuration (placeholder addresses, no secrets)
!apps/console/.env.devnet
!apps/console/.env.production
!apps/console/.env.test

# Claude Code — local/sensitive (shared config like .claude/settings.json,
# commands, agents, and skills remains tracked)
.claude/settings.local.json
.claude/*.local.json
CLAUDE.local.md
**/CLAUDE.local.md
.claude/local/

# Editor / OS
.DS_Store
.idea/
*.swp

# Logs
*.log

# App M7.3–7.4: Governance write path (vote, execute, template composer)

**Status:** DELIVERED 2026-07-30 (§10 carries the closure notes; §7's Q1–Q4
were confirmed by Ira 2026-07-30 before commit A, all as proposed).
PLANNED 2026-07-28 (delivery shape: four commits on branch
`m7.3-governance-write-path`, merged in one GitHub PR/CI cycle: **commit A** =
proto encoders + **the three-level broadcast guard and its rejection matrix**,
**commit B** = the template registry, **commit C** = the vote, execute and
compose flows, **commit D** = docs-only closure including the app-spec §12.3
amendment. Starts only after the
[7.2](2026-07-28-app-m7.2-governance-read-ui.md) PR is merged. ≈32 changed
files. **This PR carries the milestone's relay amendment and is scoped to
nothing else, so that amendment gets a focused review.**)
**Epic:** the nvHASH App — [`app-spec.md`](../specs/app-spec.md) (v1.0-RC1)
**Milestone:** M7 — Governance & admin,
[master plan](2026-07-13-app-implementation-plan.md) §2 **PRs 7.3 and 7.4,
delivered as one PR**; [milestone overview](2026-07-28-app-m7-governance.md)
**Companions:** [`SECURITY.md`](../../SECURITY.md),
[`apps/web/CLAUDE.md`](../../apps/web/CLAUDE.md),
app-spec §8.7 / §10.2–§10.3 (lifecycle, privileged-write rigor) / §12.3
(the broadcast relay) / §14.6 / §17.1 (confirmation rigor),
[`contracts/src/msg.rs`](../../contracts/src/msg.rs) (the admin `ExecuteMsg`
surface the templates target),
[`apps/console/src/pages/Admin.tsx`](../../apps/console/src/pages/Admin.tsx)
(the working admin UI the templates mirror),
[2026-07-24-app-m6.4-operator-view.md](2026-07-24-app-m6.4-operator-view.md)
§2.5 (**the two-level deep guard this extends — read it before anything else**),
[7.2 plan](2026-07-28-app-m7.2-governance-read-ui.md)

## 1. Origin & problem statement

Master plan §2 covers this work in two rows:

- **7.3** — *"Vote/execute signing: `MsgVote`/`MsgExec` through the 5.2
  lifecycle, would-fail simulation before sign."*
- **7.4** — *"Template proposal composer"*, whose original "may move post-v1"
  caveat was stale against §14.6 and is amended in the M7.0 commit.

**They are delivered as one PR (scope decision, Ira 2026-07-28).** The two rows
are the same guard. Overview D7 already fixed the end-state three-level shape
at M7.0, so staging `MsgSubmitProposal` as *present-but-provably-rejected* for
exactly one PR would be ceremony that only pays off if 7.3 shipped meaningfully
earlier than 7.4 — and it does not. Merged, the guard is written once, §12.3 is
amended once, and the rejection matrix is authored once. Split, 7.4 would
immediately re-edit `build.ts`, `proto.ts` and `broadcast-guard.test.ts` that
7.3 had just written. Combined size is ≈32 files, well inside the review
ceiling.

> **This supersedes D7's *staging*, not D7's *guard shape*.** The three-level
> design is unchanged; only the "present but rejected, then flipped on" step
> disappears, because there is no longer an intervening PR to stage it across.
>
> *[D7's GUARD SHAPE was superseded too, on 2026-07-30, by a security review of
> the finished branch — see §2.2 and §10.4. This paragraph is left as written
> because it records what was believed at planning time.]*

App-spec §8.7 asks for: members cast votes and execute-when-passed with the
§10.2 preview/sign/track lifecycle and **the decoded payload shown before
signing**; and proposal creation scoped to **selecting from decoded templates
of the program's admin actions** — config change with a diff view, halt/resume,
pause/unpause, bridge config — rather than free-form message building, which
stays a Console strength.

**This PR opens the broadcast relay to governance for the first time**, and it
discharges the constraint app-spec §14.6 and the master plan's row 6.4
amendment both record: *admin program-ops … are NOT in the relay's variant set
and are provably rejected by its rejection matrix, so delivering them later is
a deliberate design-review event rather than a config change.* **This PR is
that design-review event.**

What exists on `main`: the 5.2 lifecycle (`app/tx/{lifecycle,proto,build,
use-tx-flow,preflight.server,broadcast.server,confirm,flow-status,reasons}.ts`),
and the M6.4 two-level guard whose `guardOperatorExecute` runs five conditions
ending in **byte-identical canonical re-encode** — the condition that takes the
guard out of a parser arms race. `apps/console/src/pages/Admin.tsx` and
`src/tx/messages.ts` are a working reference for every admin message and its
danger-tier confirmation.

## 2. Mechanism

### 2.1 What joins the allowlist

`ALLOWED_MSG_TYPE_URLS` gains exactly three types:
`/cosmos.group.v1.MsgVote`, `/cosmos.group.v1.MsgExec`, and
`/cosmos.group.v1.MsgSubmitProposal`. Every other `cosmos.group.v1` type stays
rejected, and the M6.4 `MsgExecuteContract` matrix is unchanged.

### 2.2 One guard class, structural

> **REVISED 2026-07-30 at closure, after a security review of the branch.** The
> text below is the SHIPPED design. What this section originally specified —
> two guard classes, with six conditions on `MsgSubmitProposal` including a
> closed-template match per inner message, a per-element canonical re-encode and
> a live policy sweep — was built, reviewed, and then **cut**. §10.4 records why
> at length; the short form is that D7's rationale was backwards and the
> conditions bought no authority reduction. The original text is preserved in
> git history and in D7's superseded row.

All three admitted types are guarded the same way: **type URL → signer ↔
session-address binding → closed field set with bounded values → the `exec` pin
where the message has one → canonical re-encode.**

`MsgVote` carries `{proposal_id, voter, option, metadata, exec}` and `MsgExec`
carries `{proposal_id, signer}` — closed scalar payloads embedding no other
message.

`MsgSubmitProposal` carries **`messages []Any`**, and the guard reasons about
the ENVELOPE only: the single `proposers` entry must be the session address, the
field set is closed, title/summary/metadata are bounded, the message count is
1..`MAX_PROPOSAL_MESSAGES`, `exec` is pinned, and the envelope is re-encoded with
the inner `Any` bytes **passed through verbatim**.

**The inner messages are not inspected, and that is the point of the revision.**
A proposal executes nothing until the group's decision policy is satisfied by
other members voting, so the **threshold is the enforcement boundary** — and
what protects members from a hostile proposal is being able to READ it before
voting, which 7.2's decoder delivers without involving this relay.

**Why `proposers` is pinned to exactly one entry.** x/group permits several and
counts each as a required signer (§4b C1), but the relay accepts exactly one
signature, so one proposer is the only shape that can ever be valid. Pinning the
count is stricter than checking every entry — it closes the "one element decides
the verdict while another rides along" failure outright, and it leaves one
accepted encoding rather than admitting `[addr, addr]` as a second spelling.

### 2.3 The template set is closed and mirrors the contract exactly

> **SCOPE REVISED 2026-07-30 (§10.4).** Everything below still holds, but it is a
> property of the COMPOSER — what the App offers to build — not of the relay
> guard. The guard no longer matches inner messages against this set.

Templates target the admin-gated `ExecuteMsg` variants in
[`contracts/src/msg.rs`](../../contracts/src/msg.rs): `UpdateConfig` (ten
optional fields), `SetHalted`, `PauseVault`, `UnpauseVault`,
`ClearPendingDelegations`. Bridge config joins if and when §14.3 resolves —
until then its template is **absent, not stubbed** (§7 Q1).

Each template declares its parameters with the **same bounds the contract
enforces**, so the composer cannot build a message the contract will reject for
a bound violation. Preflight remains convenience only; the contract is the
enforcement boundary (`SECURITY.md`). Numeric entry follows the repo-standard
**reject-never-clamp** rule, reusing `test/amount.test.ts`'s subject.

`UpdateConfig` is the interesting one: only supplied fields change, so the
composer must distinguish "not supplied" from "supplied as the current value",
and the **diff view** (§8.7's named requirement) shows current → proposed for
exactly the fields being changed, with untouched fields visibly untouched.

**One vocabulary, three consumers.** The template registry is the single source
of admin-action semantics: 7.2's decoder reads proposals *containing* these
messages, the confirm step *discloses* them, and the composer *builds* them.
All three import from the registry, so a proposal composed by the App and read
back by the App cannot be described differently (§4 invariant 6).

### 2.4 One dangerous subtlety: the `exec` field

Both `MsgVote` and `MsgSubmitProposal` carry an `exec` field that, set to
`EXEC_TRY`, attempts execution in the same transaction. That silently turns a
vote — or a submission — into a vote **plus** execution of whatever the
proposal contains, which after this PR includes admin program-ops.

**The guard pins `exec` to the unspecified/no-try value on both messages and
rejects anything else.** Execution is always a separate, separately-confirmed
`MsgExec` with its own decoded-payload disclosure. A user who intends both
performs two confirmations. This is §17.1 confirmation rigor: the danger tier
is not something to bundle into a cheaper action.

### 2.5 Would-fail simulation before sign

Per the §10.2 lifecycle: simulate before the wallet is asked to sign, and
surface a would-fail reason through `app/tx/reasons.ts` rather than letting the
user sign into a revert. Preflight remains **convenience only** —
`SECURITY.md`: the contract, and here the group module, remain the enforcement
boundary.

The governance-specific would-fail cases worth naming: for `MsgExec` —
proposal not yet passed, voting period not closed, `min_execution_period` not
elapsed, already executed, pruned; for submission — not a member, policy not
found, parameter out of bounds.

### 2.6 The decoded payload is shown before signing

§8.7 and §10.2: the confirm step shows the human summary above the exact
message JSON, reusing 7.2's `app/governance/decode.ts` rather than a second
vocabulary. For `MsgExec` this means showing **what the proposal will
execute**, not merely "execute proposal 12" — that is the whole of Grace's
question. For submission it means the exact `MsgSubmitProposal` JSON including
every inner message.

**Danger tier, framed accurately.** Submitting a proposal is not itself the
dangerous act — passage and execution are. The confirmation says so (this
proposes X; if it passes and is executed, X happens) without using that framing
to soften the disclosure. The console's `Admin.tsx` copy is the reference for
tone and specificity.

## 3. Per-commit design

### 3.1 Commit A — encoders and the guard

- `app/tx/proto.ts` — canonical proto3 encoders for `MsgVote`, `MsgExec` and
  `MsgSubmitProposal`, matching the existing encoder discipline (field order,
  omitted defaults).
- `app/tx/build.ts` — `ALLOWED_MSG_TYPE_URLS` gains the three types;
  `GOVERNANCE_VOTE_OPTIONS` closed set; `governanceInnerJson` and
  `submitProposalInnerJson` canonical builders mirroring `operatorInnerJson`;
  `guardGovernanceMsg` (structural, §2.2) and `guardSubmitProposal` (six
  conditions, §2.2); `intentToProtoJson` and `intentSigner` extended.
- `test/broadcast-guard.test.ts` — the rejection matrix. **The PR's
  centerpiece**; see §4 invariant 1 and §9.
- `test/tx-build.test.ts` — canonical-encoding golden vectors for all three.

### 3.2 Commit B — the template registry

- `app/governance/templates.ts` — the closed registry: per template, its id,
  its canonical inner-message builder, its parameter schema with contract
  bounds, and its summary vocabulary.
- `test/governance-templates.test.ts` — bounds, canonical output, and the
  round-trip against 7.2's decoder.

### 3.3 Commit C — the flows

- `app/governance/{vote-flow.ts, execute-flow.ts, compose-flow.ts}` — intents
  through the 5.2 lifecycle via `use-tx-flow.ts`.
- `app/routes/governance.new.tsx` — the composer route, member-gated;
  `app/routes.ts`.
- `app/components/governance/{vote-panel, execute-button, template-picker,
  template-form, config-diff, submit-confirm}.tsx`.
- `app/tx/preflight.server.ts` and `reasons.ts` — the governance would-fail
  rules and vocabulary (§2.5).
- `app/i18n/locales/en.ts`; `e2e/axe.spec.ts`.
- Tests: `test/tx-preflight.test.ts` (governance matrix),
  `test/tx-confirm.test.ts` (disclosure = signed bytes, over all three msgs),
  `test/governance-flows.test.ts`, `test/governance-compose.test.ts`.
- `e2e/governance-write.spec.ts` (offline) and
  `e2e-live/governance-write.spec.ts` — the live suite closes the loop against
  7.1's `gov-drill.sh` substrate: compose → vote → execute → observe
  `executorResult`.

### 3.4 Commit D — docs-only closure

The **app-spec §12.3 amendment** (§2.1–§2.4), app-spec §8.7 and §14.6
revisions, `contracts/IMPLEMENTATION-STATUS.md` if the enumerated
admin-capability surface changes, `apps/web/CLAUDE.md`, delivery notes,
master-plan revision log.

## 4. Security & invariants (enforced mechanisms with gating tests)

1. **The relay admits exactly three new types, each only in canonical form.**
   Every other `cosmos.group.v1` type is rejected. *Gate:*
   `apps/web/test/broadcast-guard.test.ts`, extended with: each admitted type
   in canonical form accepted; unknown group types rejected; **the M6.4
   `MsgExecuteContract` matrix unchanged and still passing**.
2. ~~**`MsgSubmitProposal` passes only when every inner message is an exact
   template instance.**~~ **WITHDRAWN 2026-07-30 (§10.4).** The relay carries a
   proposal's inner messages unexamined; the group's threshold is the
   enforcement boundary and 7.2's decoder is what lets members read what they
   are voting on. The matrix now asserts these as ACCEPTANCES, so re-tightening
   is a deliberate edit to a named case. The **zero-element** row survives (a
   proposal to do nothing still consumes a voting period) and so does the
   per-proposal element cap. *Original gate, for the record:* matrix rows for an
   inner message outside the template set; one targeting a different contract; a
   zero-message proposal; one mixing a valid template with an arbitrary message;
   and a valid template with one field mutated post-encode.
3. ~~**Inner messages are canonically re-encoded individually.**~~ **WITHDRAWN
   2026-07-30 (§10.4).** Replaced by an ENVELOPE re-encode with the inner `Any`
   bytes passed through verbatim. *Gate:* the envelope-canonical case in
   `broadcast-guard.test.ts` (reordered fields, duplicated proposer, unknown
   field).
4. **`exec` is pinned on both `MsgVote` and `MsgSubmitProposal`.** Any value
   that would attempt execution inside the same transaction is rejected
   (§2.4). *Gate:* an explicit case per non-permitted `exec` value on each
   message.
5. **Sole signer is the session address.** `voter`/`signer`/the single
   `proposers` entry must equal it; a mismatch is rejected server-side, not
   merely hidden in the UI. ~~Policy addresses must be discovered program
   policies.~~ **That clause is WITHDRAWN 2026-07-30 (§10.4)** — it conferred
   nothing, and the composer still says so as a courtesy. *Gates:*
   signer-mismatch cases in `broadcast-guard.test.ts`; the standing
   session-scope suite.
6. **Compose and decode agree.** A proposal built by the composer, read back
   through 7.2's decoder, yields the same summary — one vocabulary, three
   consumers (§2.3). *Gate:* the round-trip case in
   `test/governance-templates.test.ts`.
7. **The template set matches the contract.** *(Reclassified 2026-07-30: a
   PRODUCT completeness property — a new admin capability must be reachable
   from the composer — not a security one. The gate is unchanged.)* Every template's parameter
   bounds equal the contract's, and no template exists for a variant not in
   `contracts/src/msg.rs`. *Gate:* `governance-templates.test.ts`, with a case
   that fails if a template's field set drifts from the contract schema.
   *Disproof:* the contract gaining an admin variant with **no** template — the
   drift test as described catches a template that no longer matches, but not a
   variant that never got one. The test must assert the mapping is **total in
   both directions** against the committed `cargo schema` output, so a new
   admin capability fails CI here rather than being quietly unreachable.
8. **The direct-admin path stays closed.** After this PR a *direct*
   `MsgExecuteContract` carrying an admin variant must **still** be rejected —
   admin ops reach the chain only through governance. *Gate:* the M6.4
   admin-variant rejection rows, unchanged and still passing. **This is the
   invariant most at risk of accidental erosion here.**
   *Disproof:* an admin action reaching the chain by a route the matrix does
   not model at all — a template whose inner message is itself a
   `MsgSubmitProposal` (governance nesting), a template targeting the group
   module rather than the contract (e.g. `MsgUpdateGroupMembers`, which changes
   who governs), or an `authz`/`MsgExec`-wrapped admin message. The matrix
   tests direct `MsgExecuteContract`; it must also prove these three are
   refused, because each reaches the same authority by a different path.
9. **Reject-never-clamp on every numeric input**, with contract bounds at
   entry, and `UpdateConfig` never silently changes an unsupplied field.
   *Gates:* the reused `test/amount.test.ts` subject;
   `test/governance-compose.test.ts` bounds and diff cases, including
   supplied-as-current-value.
10. **No key material; the wallet signs everything.** The flows build messages
    only; the relay forwards fully user-signed bytes; no privileged capability
    exists server-side to misuse. *Gates:* the unchanged 5.2 suites;
    `test/tx-confirm.test.ts` (disclosure = signed bytes).
11. **Preflight is convenience; the group module and the contract are the
    enforcement boundaries.** A would-fail reason never becomes an
    authorization claim, and a passing preflight never implies acceptance.
    *Gate:* `test/tx-preflight.test.ts`, including a
    preflight-passes-chain-rejects case.
12. **Confirmation rigor on privileged writes** (§17.1): consumer summary plus
    exact JSON on every flow; `MsgExec` discloses **what the proposal will
    execute**; submission discloses every inner message; danger-tier framing is
    accurate about proposal-vs-execution (§2.6). *Gates:*
    `test/tx-confirm.test.ts`; confirm assertions in the offline e2e.
13. **All inputs validated and bounded at entry.** `proposal_id` u64, `option`
    a closed enum, metadata length-bounded, template parameters
    contract-bounded. *Gate:* `test/tx-preflight.test.ts`; the guard field-set
    checks.
14. **Size and rate caps unchanged.** The existing relay caps apply to the new
    types with no exemption. *Gate:* the standing relay caps suite.
15. **Standing gates continue:** axe on the new route, i18n coverage,
    `check:bundle`, `pnpm -r` typecheck/test.

## 4b. Completeness obligations

Per [overview §7](2026-07-28-app-m7-governance.md). For this PR C1 is not a
persistence question — it is a **guard** question, and it is where a missed
cell is an exploit rather than a display bug.

### C1 — Cardinality inside guarded messages

| Collection | Max multiplicity | Guard obligation |
| --- | --- | --- |
| `MsgSubmitProposal.messages[]` | **N > 1**, unbounded by the module | Every element validated and canonically re-encoded **individually**; a per-element bound is enforced and an over-bound proposal is rejected, not truncated |
| `MsgSubmitProposal.messages[]` = **0** | legal on the wire | **Explicitly rejected** — an empty proposal is not a template instance |
| `proposers[]` | **N > 1** | **Every** entry must equal the session address, not just the first |
| Messages per **transaction** | **N > 1** — a tx may carry several governance messages | The guard runs over **every** message in the body; a tx that pairs one valid `MsgVote` with one unguarded message is rejected |
| `UpdateConfig` optional fields | 10, each present-or-absent → **2¹⁰ shapes** | The canonical builder omits absent fields; the re-encode comparison makes the shape space irrelevant to the guard, which is why condition 5 exists |

> *Disproof for the whole C1 block:* any construction where the guard's verdict
> is decided by **one** element of a collection while another element rides
> along unchecked. M6.4's batched-payment P1 was that failure in a decoder,
> where the cost was lost history; here the cost is an arbitrary message
> executed by the contract's admin. The matrix rows in §4 invariant 2 —
> zero-element, mixed valid+arbitrary, and post-encode mutation — exist
> specifically to falsify this.

### C2 — Wire bounds

Consumes 7.1's `packages/api-types/src/bounds.ts`. New bounds this PR
declares there rather than inline: `MAX_PROPOSAL_MESSAGES` (the guard's
per-proposal element cap), `MAX_PROPOSAL_METADATA_LEN`, `MAX_VOTE_METADATA_LEN`
if Q3 keeps it. **A guard bound written as a literal in `build.ts` is a review
failure** — the composer, the guard and the reader must not be able to disagree
about the same limit.

### C3 — Concurrency

| Read-then-write | Remedy |
| --- | --- |
| Double-submit of the same proposal (user double-clicks, or retries a timed-out broadcast) | Idempotent by construction on-chain — a second `MsgSubmitProposal` creates a **second proposal**, which is a real hazard rather than a duplicate row. The flow disables re-submit after broadcast and the confirm step states that submission is not idempotent |
| Vote raced against voting-period end | Chain-enforced; preflight surfaces it, chain decides (invariant 11) |
| `MsgExec` raced by two callers | Chain-enforced — the second fails as already-executed; the UI must render that as a benign outcome, not an error state |

No application-level uniqueness or cap enforcement exists in this PR, so C3's
default remedy (DB constraint) does not apply.

### C4 — State × affordance

Inherits 7.2's table and fills the affordance column that 7.2 left uniformly
read-only. **This is the row M6.4 got wrong** — an action panel rendered
against a state the action could not validly operate on.

| Proposal state | vote | execute | notes |
| --- | --- | --- | --- |
| open, not yet voted, session is member | **offered** | hidden | |
| open, already voted | hidden (x/group forbids re-vote — [VERIFY] via 7.1's drill) | hidden | if the chain *accepts* re-votes, this cell becomes "offered, as a change" |
| open, session not a member | hidden, with the reason stated | hidden | never a disabled control with no explanation |
| open, anonymous | hidden; connect prompt | hidden | |
| accepted, `min_execution_period` pending | hidden | **shown, disabled**, with the eligible-at time | disabled-with-reason, never hidden — the user needs to know it is coming |
| accepted, executable | hidden | **offered** (Q2 decides member-only vs anyone) | danger-tier confirm |
| executed (SUCCESS or FAILURE) | hidden | hidden | FAILURE is terminal; x/group does not permit re-exec |
| rejected / aborted / withdrawn | hidden | hidden | |
| **pruned** | hidden | hidden | no action can reference a proposal the chain discarded |
| `groupVersion` ≠ current | hidden | per state above | membership changed; voting is moot on a closed proposal |

*Gate:* a table-driven case per row in `test/governance-flows.test.ts`, plus
the e2e assertion that no action control renders for any terminal state.

### C5 — Plane precedence

Unchanged from 7.2's table for display. The one addition: **affordances are
decided from the live plane only.** An action must never be offered on the
strength of an indexed row that a live read has not confirmed — a stale
"accepted" that has since been executed would offer an execute button that
always fails. When the live read is down, actions are **hidden with the reason
stated**, never rendered optimistically. *Gate:*
`test/governance-flows.test.ts` live-down case.

## 5. Commits

| # | Component | Scope |
| --- | --- | --- |
| A | apps/web | proto encoders, allowlist, both guards, **the rejection matrix**, golden vectors |
| B | apps/web | the closed template registry + its bounds/canonical/round-trip suite |
| C | apps/web | vote, execute and compose flows, composer route, config diff, preflight, i18n, offline + live e2e |
| D | docs | **app-spec §12.3 amendment**, §8.7/§14.6 revisions, `apps/web/CLAUDE.md`, delivery notes, revision log |

## 6. Same-change doc updates

- **`docs/specs/app-spec.md` §12.3** — the recorded allowlist amendment,
  written **once** for all three types (commit D). It must state the admitted
  set, both guard classes, the six `MsgSubmitProposal` conditions, the `exec`
  pin, that the direct-admin `MsgExecuteContract` variants remain rejected, and
  that extending the set stays a design-review event.
- `docs/specs/app-spec.md` §8.7 and §14.6 — member actions and template-scoped
  creation shipped; admin program-ops now originate as governance templates.
- `contracts/IMPLEMENTATION-STATUS.md` — only if the enumerated
  admin-capability surface changes. `SECURITY.md` makes every admin capability
  a spec-level event, so confirm the enumeration is exhaustive in this pass.
- `apps/web/CLAUDE.md`; master-plan revision log.

## 7. Open questions

*Milestone decisions are in [the overview](2026-07-28-app-m7-governance.md) §4.*

- **D19 / Q1 — which templates ship in v1?** §8.7 names config change,
  halt/resume, pause/unpause and bridge config. Bridge config depends on §14.3
  (external, NUVA), which keeps the Market surfaces as shells. Proposed: ship
  the four contract-backed templates and **omit** the bridge-config template
  entirely rather than stub it — an absent template is honest, a disabled one
  invites "when". Confirm before commit B.
- **Q2 — is `MsgExec` offered to non-members?** Execution in `x/group` is
  permissionless once a proposal has passed. Proposed: offer it to any
  connected wallet with the danger-tier confirmation, and say plainly in the UI
  that execution is permissionless. Confirm before commit C — it changes the
  gating, not the guard.
- **Q3 — vote and proposal `metadata`.** x/group allows free-form metadata on
  both. Proposed: **omit on votes** (nothing consumes it) and **offer a bounded
  plain-text field on proposals** (a human rationale is genuinely useful and
  7.2 renders it), with the UI clear that it is public and permanent. Confirm
  before commit A — it changes the guards' field sets.
- **Q4 — multi-message proposals.** x/group permits several messages per
  proposal. Proposed: v1 composes **one** template per proposal, while the
  guard still validates element-wise so multi-message proposals submitted
  elsewhere read correctly in 7.2. Confirm before commit A; allowing several
  later is additive, restricting later is not.
- **Q5 — would-fail simulation depth for `MsgExec`.** A full simulation
  executes the proposal's inner messages. Proposed: simulate and surface the
  inner failure reason, since "passed but will fail to execute" is exactly what
  7.1's `executorResult` column exists to make visible. Confirm the simulation
  is side-effect-free against the node.
- **Q6 — does the composer surface `min_execution_period`?** A passed proposal
  is not immediately executable. Proposed: show it on the confirm step so the
  proposer knows the real timeline.

**Pre-build refinement gate:** revise §3 and Q1–Q6 in a same-file revision once
7.2 is merged and §14.3's status is known.

## 8. Implementation guidance (delegated executor)

**Read first, in order:** `SECURITY.md`;
**`2026-07-24-app-m6.4-operator-view.md` §2.5 and §4** — the guard idiom this
PR extends and the reasoning for each of its five conditions;
[the overview](2026-07-28-app-m7-governance.md) §4 D7; this document; app-spec
§10.2–§10.3, §12.3, §17.1; `contracts/src/msg.rs` in full for the admin
variants; `apps/console/src/pages/Admin.tsx` and `src/tx/messages.ts` as the
working reference; `apps/web/CLAUDE.md`.

**Ordering.** Commits A and B before C, without exception: **no flow exists
until the guard, the template registry and the rejection matrix pass.** Never
land a bare type-URL allowlist entry, even transiently within the working tree.

**Hard rules.**

- **Never create or push commits.** Ira reviews and commits.
- **The guard is not optional or reorderable.** Conditions run in the specified
  order, and the canonical re-encode is last and non-negotiable.
- **Every inner message is canonically re-encoded and compared byte-for-byte.**
  Structural validation is conditions 1–4 and is not sufficient alone.
- **Do not open the direct-admin path.** Admin variants must remain rejected as
  direct `MsgExecuteContract` messages after this PR (invariant 8).
- **Do not relax the `exec` pin** for convenience.
- **Do not add a template for a variant not in `contracts/src/msg.rs`,** and do
  not widen a parameter's bounds past the contract's.
- **Do not weaken a test to make it pass.**

**Stop-and-ask triggers:** any allowlist entry beyond the three named types;
any additional field in a guarded message, however convenient; any template
whose bounds you cannot trace to a contract check; any need to relax a guard
condition to make a legitimate message pass (that is a signal the canonical
builder is wrong, not the guard); any suggestion to bundle vote and execute
into one signature; any test weakened to pass.

## 9. PR & review process

One GitHub PR titled `app/M7.3–7.4 …`, referencing this plan, both master-plan
rows and the §12.3 amendment.

**Spend the overwhelming majority of review effort on invariants 1, 2, 3 and
8.** Attempt to construct a `cosmos.group.v1` message that passes the guard but
is not an admitted type in canonical form: a `MsgSubmitProposal` shaped to look
like a vote; a `MsgVote` with `exec` set; an extra inner message appended after
a valid one; an inner message targeting a different contract or a different
admin variant; a valid template with one field mutated post-encode; a proposal
for a policy that is not a program policy; a proposer that is not the signer;
duplicate JSON keys, nested or smuggled fields, non-canonical field ordering.
The rejection matrix must already cover every attempt before the flows are
reviewed at all.

Then separately confirm the M6.4 direct-admin rejection rows still pass. The
whole point of this design is that admin ops reach the chain **only** through
governance, and the easiest way to lose that is to relax the older matrix while
extending the newer one.

Third priority is invariant 12 — confirm that `MsgExec`'s disclosure shows what
the proposal actually executes, since that is the difference between an
informed signature and a blind one.

The live-devnet suite runs against 7.1's `gov-drill.sh` substrate.

## 10. Delivery notes (DELIVERED 2026-07-30)

**§7's open questions, confirmed by Ira 2026-07-30 before commit A**, all four as
proposed: Q1 — the five contract-backed templates (one per `ADMIN_VARIANTS`
entry, which invariant 7's totality requires), bridge config **omitted
entirely**; Q2 — `MsgExec` offered to any connected wallet, with the UI stating
that execution is permissionless; Q3 — vote metadata **omitted** and pinned
absent by the guard, proposal metadata offered as a bounded public field; Q4 —
one template per proposal, guard still element-wise. Q5 and Q6 were adopted as
proposed without a separate confirmation, since neither changes a guard.

### 10.1 Three things the plan did not have right

**The guard had to become async, and §2.2 condition 3 is why.** "Is this
`group_policy_address` one of the discovered program policies" is a live,
set-valued read (D1). The plan wrote the six conditions as one ordered guard —
correctly — but left `guardSignedTx` synchronous, and the three ways out were:
hardcode a policy address (the topology assumption `SECURITY.md` forbids), move
condition 3 into the route (splitting a guard whose ordering §8 calls
non-negotiable), or make the guard async. It is async, the read happens **only**
for a transaction that actually carries a submission, and an unresolvable or
**truncated** policy sweep rejects **503** — a status the plan did not name,
chosen because the submission may be perfectly well-formed and "we could not
verify the policy set" is the honest thing to say.

**Guard 6 had to become per-shape, and this is the near-miss worth recording.**
Through M6.4 every carried message held its signer in field 1 (`owner` /
`sender`), so one check outside the type dispatch served all of them. `MsgVote`
field 1 is a **varint proposal id** and `MsgSubmitProposal` field 1 is the
**policy address**. Left as it was, the outer check would have compared a
proposal id against a bech32 address and rejected every governance message —
which from outside looks exactly like a guard working. Each governance guard now
performs its own binding (`voter`, `signer`, and **every** entry of
`proposers`), and the field-1 check runs only for the shapes it is about.

**§4b C5's rule needed a field the plan did not anticipate.** "Affordances are
decided from the live plane only" cannot be answered by `plane`: for a CLOSED
proposal the honest display plane is the mirror (x/group prunes), so execute —
which exists precisely for accepted proposals — would have been permanently
hidden. `ProposalDetailVM.liveState` is therefore a separate nullable field
meaning "the chain, just now, confirmed the state we are about to act on", and
`loadGovernanceProposalData` now live-reads accepted proposals too. That widening
pays for itself: a successfully executed proposal is pruned in its own
transaction, so a failed live read on an accepted proposal is **itself** evidence
not to offer the button.

### 10.2 Smaller decisions taken during the build

- **The inner message's `sender` is pinned to the POLICY address**, not the
  proposer. x/group executes a proposal's messages as the policy account, which
  is the contract's admin; a message with any other sender could never execute.
  The plan's condition 4 named the type URL, contract, variant and field set;
  the sender belongs in the same list and is now checked and canonically
  re-encoded with them.
- **`update_config` supplying NOTHING is refused.** Every field is optional, so
  the contract would accept it and change nothing — a governance vote on a
  no-op. Refused at the boundary rather than composed.
- **`pause_vault.reason` is bounded 1..256** — a deliberate narrowing past the
  contract, which accepts an empty string. The reason is rendered to users as
  the vault's `pausedReason`, and "paused, no reason given" is the state
  `SECURITY.md`'s never-lie rule exists to prevent. Recorded here because §8
  forbids widening a bound past the contract's and this narrows one.
- **Uints above `Number.MAX_SAFE_INTEGER` cannot be composed.** A representation
  bound, narrower than the contract's `u64`, and stated rather than hidden:
  `JSON.stringify` cannot emit one without losing precision, and a canonical
  form that rounded its own input would defeat condition 5. It binds only two
  second-counts where the ceiling is ~285 million years.
- **The template registry's imports are TYPE-ONLY**, because `build.ts` imports
  it at runtime for the guard and the relay decodes untrusted bytes through
  `build.ts`. `templateSummaryKey` therefore returns an i18n KEY plus its params
  rather than a translated string — which also makes invariant 6's round trip a
  comparison of two resolved strings rather than of two tables.
- **`test/governance-compose.test.ts` did not gain the composer cases.** §3.3
  listed it; the bounds, canonical-output, round-trip and diff cases all landed
  in `test/governance-templates.test.ts` instead, because they are properties of
  the registry and splitting them across two files would have separated a bound
  from the declaration it tests. 7.2's `governance-compose.test.ts` is unchanged.
- **The offline e2e write assertions went into `e2e/governance.spec.ts`, not a
  separate `e2e/governance-write.spec.ts`.** §3.3 named the second file. Offline
  the corpus is ungoverned and there is no session, so every write assertion is
  necessarily about an ABSENCE — no action control in any state, the composer's
  anonymous gate — and those read as amendments to the existing "renders
  anonymously" spec rather than as a suite of their own. Two of that spec's
  assertions had to change with this PR anyway (the read-only note is now false,
  and one connect prompt now legitimately exists inside the actions section),
  which is the same file. `e2e-live/governance-write.spec.ts` IS separate, as
  planned, because that is where the write path has a chain to act on.
- **`e2e-live/governance-write.spec.ts` needs a new key,
  `E2E_LIVE_GOV_MEMBER_KEY`** — a funded throwaway devnet key that is also a
  group member. The M6.4 precedent exactly: proposing and voting are
  membership-gated, so the generic `E2E_LIVE_SIGNER_KEY` cannot cover them any
  more than it could cover the enrol leg. Absent, those legs skip loudly and the
  gate assertions still run.
- **The canonical goldens for the three governance messages are HEX, not
  captured-transaction hashes.** §3.1 asked for golden vectors; the operator
  form (re-encode a captured devnet tx, require the hash to match) is not
  available because no governance transaction the App built exists in the corpus
  — 7.1's drill used the CLI. The hex goldens pin field order, the `exec`
  omission and the omitted-default rules so a re-encode is a visible diff;
  proving the canonical form is the ACCEPTED form remains `e2e-live`'s job until
  a transaction is captured. Worth a fixture-capture follow-on.

### 10.4 The guard was cut after review, and D7's rationale was the defect

**RECORDED 2026-07-30, after a security review of the finished branch (Ira).**
This is the most consequential thing in the PR, and it is a PLAN defect rather
than a code defect — the code faithfully implemented what D7 specified.

**What was built and then removed.** `guardSubmitProposal` shipped with six
conditions: type URL, proposer binding, **a live sweep proving the
`group_policy_address` was one of the program's discovered policies**, **a match
of every inner `Any` against the closed template set**, **a byte-identical
canonical re-encode per inner message**, and the `exec` pin. Conditions 3, 4 and
5 are gone. What remains is structural (§2.2).

**The defect.** D7 justified those conditions by analogy: a plain type-URL entry
*"would open the relay to arbitrary messages from the policy account — strictly
worse than the `MsgExecuteContract` hole M6.4 closed."* **That comparison is
backwards.**

| | unguarded `MsgExecuteContract` (M6.4's hole) | unguarded `MsgSubmitProposal` |
| --- | --- | --- |
| When does anything happen | **on inclusion** | only if the group's decision policy is satisfied |
| Under whose authority | the signer's own | the policy account's, after N members vote yes |
| What stops it | nothing but the guard | **the group's threshold** |

The two are not comparable, and the proposal case is *less* dangerous in the
only dimension that matters — whether anything happens without further human
approval. M6.4's guard closed a hole; this one was guarding a suggestion box.

**Two further facts made the removed conditions redundant, not merely
expensive.** x/group declares `proposers` its required signer (and `voter` for
`MsgVote`), so with the relay's pre-existing sole-signer guard the CHAIN already
rejects a foreign proposer — condition 2 restates it rather than adding it. And
**what actually protects members from a hostile proposal is reading it before
they vote**: 7.2's decoder summarizes a closed union and tags everything else
`unknown` with the exact JSON. That is a read-surface property and it does not
involve this relay at all.

**What the removal cost and bought.** Gone: an async relay guard, a live chain
read on every submission, a 503 availability failure mode, and a registry the
relay had to keep in lockstep with `contracts/src/msg.rs`. Kept: the three-type
allowlist closure, the session binding, the closed field set and text bounds,
the message-count cap, the `exec` pin, and an envelope re-encode. The template
registry survives as the **composer's** vocabulary — which is what §8.7 asked
for in the first place.

**The `exec` pin is retained on honest terms.** It is a **confirmation-rigor**
control (§17.1), not an authorization one: it means a user who intends to vote
and execute performs two confirmations. It is not load-bearing against an
adversary — the same actor can submit, vote and execute in three separate
relayed transactions, and where a policy's threshold is met by the proposer
alone, that member already holds unilateral admin authority by construction.
The review surfaced a batched submit+vote+exec body that routes around the pin;
it was assessed and **not** treated as a vulnerability for exactly that reason.

**The §4b reading.** This is a P1-class plan defect in a category C1–C6 does not
name — not a missed cell in a discrete space, but a **wrong threat model
faithfully implemented and gated by tests that all passed**. That is precisely
the failure mode overview §7.1 identifies as the one §4 cannot catch ("a
named-and-gated wrong assumption *looks verified*"), reproduced at milestone
scale: 73 matrix cases, every invariant gated, CI green, and the control was
protecting against the wrong thing. §7.2's reading rule says a P1 outside the
named categories means the list is incomplete. The honest amendment is not a
seventh completeness cell — it is that **§4 should require each invariant to
name what an attacker gains if it does not hold**, which is the question that
collapsed conditions 3/4/5 in one sentence and which no §4b table asks. Proposed
for M8's plan review; not adopted unilaterally here.

### 10.5 The execution window: one real defect, found by CI review

**FIXED 2026-07-30 (PR #25, Greptile P1 on `preflight.server.ts`).** The
observation was correct and the finding is the only one of the review's that
survived: the execute affordance and the preflight that gates it were reading
`min_execution_period` **from different places**. The loader filled the
affordance from the MIRROR'S SNAPSHOT of the decision policy
(`gov_proposals.decision_policy`, the D3 column) while `runGovernancePreflight`
read the **LIVE** policy. After any change to a policy's execution window the
two disagreed: a proposal the chain would execute rendered as pending, or one
the chain would refuse advanced to simulation.

**The prescribed direction was backwards, and the code was changed the other
way.** The finding asked for preflight to use the snapshot. Preflight was
already right: **x/group's `Proposal` carries no decision policy at all**
(`GroupProposal`, `packages/chain-client/src/group.ts`), so at execution time
the module has exactly one place to obtain `min_execution_period` — the policy
account as it stands then. The snapshot is the App indexer's own column, added
at D3 so a historical proposal's THRESHOLD stays renderable once the live policy
moves; it is not what the module consults.

**Watch the asymmetry — it is what made this subtle.** `voting_period_end` IS
snapshotted on the proposal, as an absolute instant fixed at submit.
`min_execution_period` is not. So "use the snapshot for both windows" and "use
live for both" are each wrong, and the code has to hold both facts at once.

**The fix makes the mistake unrepresentable rather than merely corrected.**
`minExecutionPeriod` moved INTO `LiveProposalState` — the field that is by
definition "what the chain confirmed just now" and the only thing §4b C5 permits
an affordance to be decided from — and the standalone `AffordanceInput` field is
gone. A caller holding a stale snapshot now has nowhere to put it, and
`test/governance-flows.test.ts` carries a case that fails if the field ever
comes back.

**RECORDED AS A DRILL GAP, not as measured fact.** The devnet policy runs
`min_execution_period: 0` (7.1 plan §3.1), so the window never bit and
`packages/fixtures/fixtures/manifest.json`'s `drill_observations` do not cover
which policy the module reads at exec time. The reasoning above is structural
and repo-checkable, but 7.1's own history is that **four** plan assumptions about
x/group were contradicted by the drill. **Proposed follow-on:** a `gov-drill.sh`
case that sets a non-zero `min_execution_period`, accepts a proposal, updates the
policy's window, and records whether execution eligibility moves — plus whether
an ACCEPTED (not merely SUBMITTED) proposal survives a policy update at all,
which the current `abort_outcome: "not-reachable-on-this-build"` observation
leaves open. Until that runs, this is a reasoned claim, not a measured one.

### 10.3 §4b's ceremony detector, for this PR

Per the overview §7.5 rule, recorded at closure — did each filled cell **change
the implementation**?

- **C1 — YES, twice.** "Every entry of `proposers`, not just the first" and
  "zero messages is legal on the wire and explicitly rejected" are guard
  conditions that exist because the table demanded them, and both are named
  matrix rows. The per-proposal element cap is a third.
- **C2 — YES.** `WRITE_READ_BOUNDS` is a new mechanism: 7.1's pairing covered
  producer↔consumer on the READ side only, and a composed proposal that exceeded
  a read bound would have been mirrored truncated-and-flagged — PR #19's defect
  one boundary further along.
- **C3 — YES, modestly.** "DB constraint, or state why not" has no DB here, and
  stating why produced the confirm line that says submission is not idempotent
  plus the disabled re-submit, rather than a flow that tried to deduplicate.
- **C4 — YES, and this is where it earns its probation.** 7.2 filled it and it
  changed nothing; here it became a pure function and a case-per-row suite
  instead of conditions in JSX. Two rows would not have been written otherwise:
  membership-**unknown** as distinct from not-a-member, and
  disabled-with-an-unknown-time for a `min_execution_period` this build cannot
  parse.
- **C5 — YES.** It produced `liveState` and the accepted-proposal live read
  (§10.1), which is a mechanism a reviewer can check.

*Disproof lines that became runnable:* invariant 8's three alternate routes
(nested proposal, group-module message, authz wrapper) are one matrix case;
invariant 7's "a variant that never got a template" is the both-directions
totality assertion against the committed `cargo schema`; §4b C1's "a verdict
decided by one element while another rides along unchecked" is the
second-proposer case. That is three of the PR's disproof lines becoming tests,
which is the ratio §7.5 asked to be counted.

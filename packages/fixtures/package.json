{
  "name": "@nvhash/fixtures",
  "version": "0.0.0",
  "private": true,
  "description": "Devnet-captured event/query fixture corpus for the nvHASH App — provisional against the pre-release vault (feature probe, not version pin); re-vetted at implementation-plan PR 8.0",
  "type": "module",
  "files": [
    "fixtures"
  ],
  "exports": {
    "./manifest": "./fixtures/manifest.json",
    "./*": "./fixtures/*.json"
  },
  "scripts": {
    "capture": "scripts/capture-fixtures.sh",
    "check": "scripts/capture-fixtures.sh --check"
  }
}
